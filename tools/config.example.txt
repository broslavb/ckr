# Скопіюй цей файл у tools/config.txt і заповни.
# config.txt у git НЕ потрапляє — паролі лишаються тільки на цій машині.

# ── Пошта, куди avto.pro шле сповіщення ──────────────────────────────────
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_FOLDER=INBOX
IMAP_USER=твоя-нова-скринька@gmail.com
# УВАГА: не звичайний пароль від Google, а «пароль додатка» —
# myaccount.google.com → Безпека → Двоетапна перевірка → Паролі додатків
IMAP_PASS=xxxx xxxx xxxx xxxx

# Необов'язково: обробляти лише листи від цього відправника.
# Якщо скринька окрема і сторонніх листів там немає — лиши порожнім.
MAIL_FROM=

# ── Доступ до бази CKR (той самий логін, що і в застосунку) ──────────────
FB_EMAIL=broslav.b@gmail.com
FB_PASS=
